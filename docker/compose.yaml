# Paths here are relative to this file's directory, so the repository root is `..`.
# Run it from the repo root: `.env` there sets COMPOSE_FILE, so plain `docker compose up` finds
# this file. Without a .env, pass it explicitly: `docker compose -f docker/compose.yaml up`.
name: dashboard

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-dashboard}
      POSTGRES_USER: ${POSTGRES_USER:-dashboard}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dashboard}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-dashboard} -d ${POSTGRES_DB:-dashboard}"]
      interval: 5s
      timeout: 3s
      retries: 20

  mongo:
    image: mongo:7
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - mongodata:/data/db
    healthcheck:
      test: ["CMD-SHELL", "mongosh --quiet --eval 'db.adminCommand(\"ping\")'"]
      interval: 5s
      timeout: 3s
      retries: 20

  web:
    build: &app_build
      context: ..
      dockerfile: docker/Dockerfile
    command: python src/manage.py runserver 0.0.0.0:8000
    environment: &app_env
      DJANGO_DEBUG: "true"
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB:-dashboard}
      POSTGRES_USER: ${POSTGRES_USER:-dashboard}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dashboard}
      MONGO_URI: mongodb://mongo:27017
      MONGO_DB_NAME: ${MONGO_DB_NAME:-dashboard}
    volumes:
      - ..:/app
    ports:
      - "8000:8000"
    # A transient import error (a half-saved settings file during development) should not leave a
    # dead container behind — the autoreloader cannot recover from a crash at startup.
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy

  worker:
    build: *app_build
    command: python src/manage.py run_worker
    environment: *app_env
    volumes:
      - ..:/app
    # A transient import error (a half-saved settings file during development) should not leave a
    # dead container behind — the autoreloader cannot recover from a crash at startup.
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      mongo:
        condition: service_healthy

  scheduler:
    build: *app_build
    command: python src/manage.py run_scheduler --loop
    environment: *app_env
    volumes:
      - ..:/app
    # A transient import error (a half-saved settings file during development) should not leave a
    # dead container behind — the autoreloader cannot recover from a crash at startup.
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
  mongodata:
