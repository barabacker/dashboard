Django>=5.2,<5.3
django-unfold>=0.91
django-leaflet>=0.34
Pillow>=11

# Только для офлайн-подложки карты (tools/make_tiles.py).
# С обычным OSM-слоем не нужны.
matplotlib>=3.10
basemap>=2.0
basemap-data>=2.0
